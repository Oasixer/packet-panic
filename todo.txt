1) get accordion working and looking right incl. taking an icon
2) make a packets accordion

3) add to the packets datastructure: bytes per field + readable version of each


import { JSX } from "preact";

{
  titleText?: string,
  titleIcon?: JSX.element,
}

export const addRow = ()



Wireshark filter!
 !dns && (udp || tcp) && !quic && !mdns && !ntp && (ip.host matches "69\.69\.69.*" ||  ip.host == 192.168.2.216)
  !dns && (udp || tcp) && !quic && !mdns && !ntp && (ip.host matches "69\.69\.69.*" ||  ip.host == 192.168.2.216) && !(ip.host matches "^3.*" || ip.host matches "^14\..*" || ip.host matches "^5.*")


!dns && (udp || tcp) && !quic && !mdns && !ntp && (ip.host matches "69\.69\.69.*" ||  ip.host == 192.168.2.216) && !(ip.host matches "^3.*" || ip.host matches "^14\..*" || ip.host matches "^5.*" || ip.host matches "^15.*" || ip.host matches "^172\.253.*")
